Setup
git init
git clone <url>

Saving Work
git add .
git commit -m "message"

Send to GitHub
git push
git pull

Receiving from Github
git branch <name>

Branching
git checkout <name>
git checkout -b <name>
git merge <branch-name>

Merging
git merge <branch-name>

